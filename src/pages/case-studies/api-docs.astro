---
import Base from '../../layouts/Base.astro';

const docs = [
  { title: 'API Reference (v1)', url: '/docs/api-reference-v1.html' },
  { title: 'SDK Quickstart – JavaScript', url: '/docs/sdk-quickstart-js.html' },
  { title: 'SDK Quickstart – Python', url: '/docs/sdk-quickstart-python.html' },
  { title: 'Versioning & Deprecation Guide', url: '/docs/api-versioning.html' }
];

const outcomes = [
  'Improved developer onboarding time by 60%',
  'Reduced support requests for API integration by 40%',
  'Introduced versioned docs for breaking changes',
  'Enabled interactive SDK examples and quickstarts'
];
---

<Base title="API & SDK Documentation Hub – Case Study">
  <section class="max-w-5xl mx-auto py-16 px-6">
    <h1 class="text-4xl font-bold text-indigo-600 mb-4">
      ⚙️ API & SDK Documentation Hub – Case Study
    </h1>

    <p class="text-lg text-gray-700 dark:text-gray-300 mb-8">
      I designed and implemented a structured API &amp; SDK documentation hub to unify how developers
      consume backend services and internal tools. The hub provides versioned API references,
      language-specific SDK guides, and interactive quickstarts to reduce integration friction.
    </p>

    <h2 class="text-2xl font-semibold text-indigo-500 mb-4">Key Outcomes</h2>
    <ul class="list-disc pl-6 mb-10 text-gray-700 dark:text-gray-300 space-y-2">
      {outcomes.map((o) => <li>{o}</li>)}
    </ul>

    <h2 class="text-2xl font-semibold text-indigo-500 mb-6">Documentation Samples</h2>
    <div class="grid md:grid-cols-2 gap-6">
      {docs.map((d) => (
        <article class="p-6 bg-white dark:bg-gray-900 rounded-2xl shadow-lg">
          <h3 class="text-xl font-semibold mb-2">{d.title}</h3>
          <p class="text-sm text-gray-500 mb-4">Open documentation sample:</p>
          <a
            href={d.url}
            target="_blank"
            rel="noopener noreferrer"
            class="px-4 py-2 bg-indigo-600 text-white rounded-full font-semibold hover:bg-indigo-700 transition"
          >
            Open
          </a>
        </article>
      ))}
    </div>
  </section>
</Base>
